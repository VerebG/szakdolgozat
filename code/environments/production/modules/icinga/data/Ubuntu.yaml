---
#Common variables
icinga::config_dir: '/etc/icinga2'
icinga::service_name: 'icinga2'
icinga::default_package_provider: apt

#Varibles for Ubuntu install
icinga::server::install::ubuntu::packages:
  - 'icinga2'
  - 'icinga-plugins'
icinga::server::install::ubuntu::ppa_repo: 'ppa:formorer/icinga'